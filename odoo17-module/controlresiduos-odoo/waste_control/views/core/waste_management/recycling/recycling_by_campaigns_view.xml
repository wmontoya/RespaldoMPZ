<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <!-- * Tree View * -->
    <record id="recycling_by_campaigns_tree_view" model="ir.ui.view">
      <field name="name">waste_control.recycling_by_campaigns.tree</field>
      <field name="model">waste_control.recycling_by_campaigns</field>
      <field name="arch" type="xml">
        <tree string="Reciclaje por campañas">
          <!-- Main fields -->
          <field name="place" />
          <field name="date" widget="date" />
          <field name="tons" />

          <!-- Relations -->
          <field name="districts_id" />
          <field name="recycle_programs_id" />
          <field name="historic_vehicles_id" string="Número de placa" />

          <!-- Computed fields -->
          <field name="month" />
          <field name="year" widget="char" />

          <field name="create_uid" string="Fuente" />
          <field name="create_date" />
          <field name="active" widget="boolean_toggle" />
        </tree>
      </field>
    </record>

    <!-- * Form View * -->
    <record id="recycling_by_campaigns_form_view" model="ir.ui.view">
      <field name="name">waste_control.recycling_by_campaigns.form</field>
      <field name="model">waste_control.recycling_by_campaigns</field>
      <field name="arch" type="xml">
        <form string="Reciclaje por campañas">
          <header />
          <sheet>
            <group>
              <!-- Main fields -->
              <group string="Información principal">
                <field name="place" placeholder="Ingrese el lugar" />
                <field name="date" widget="date" placeholder="Selecciona la fecha" />
                <field name="tons" placeholder="Ingrese toneladas" />
              </group>

              <!-- Relations -->
              <group string="Referencias">
                <field name="districts_id" placeholder="Escriba o seleccione" />
                <field name="recycle_programs_id" placeholder="Escriba o seleccione" />
                <field name="historic_vehicles_id" placeholder="Escriba o seleccione" />
              </group>

              <!-- Computed fields -->
              <group string="Campos calculados">
                <field name="month" />
                <field name="year" widget="char" />
              </group>
            </group>
            <notebook>
              <!-- * Audit fields*  -->
              <page string="Información de modificación">
                <group>
                  <group>
                    <field name="create_date" readonly="1" />
                    <field name="write_date" readonly="1" />
                  </group>
                  <group>
                    <field name="create_uid" string="Fuente"
                      readonly="1" />
                    <field name="write_uid" string="Editor" readonly="1" />
                  </group>
                </group>
              </page>
            </notebook>
          </sheet>
          <!-- * Notes/Messages  * -->
          <div class="oe_chatter">
            <field name="message_ids" />
          </div>
        </form>
      </field>
    </record>


    <!-- * Kanban View * -->
    <record id="recycling_by_campaigns_kanban_view" model="ir.ui.view">
      <field name="name">waste_control.recycling_by_campaigns.kanban</field>
      <field name="model">waste_control.recycling_by_campaigns</field>
      <field name="arch" type="xml">
        <kanban>
          <field name="id" />
          <field name="date" />
          <field name="place" />
          <field name="tons" />
          <templates>
            <t t-name="kanban-box">
              <div class="oe_kanban_global_click">
                <div class="oe_kanban_details">
                  <span class="o_kanban_record_title">
                    <field name="place" />
                  </span>
                  <div>
                    <field name="date" />
                  </div>
                  <div>
                    <field name="tons" />Toneladas</div>
                </div>
              </div>
            </t>
          </templates>
        </kanban>
      </field>
    </record>


    <!-- * Pivot View * -->
    <record id="recycling_by_campaigns_pivot_view" model="ir.ui.view">
      <field name="name">waste_control.recycling_by_campaigns.pivot</field>
      <field name="model">waste_control.recycling_by_campaigns</field>
      <field name="arch" type="xml">
        <pivot string="Reciclaje por campañas" display_quantity="true">
          <field name="districts_id" type="row" />
          <field name="date" interval="year" type="col" />
          <field name="date" interval="month" type="col" />
          <field name="tons" type="measure" />
        </pivot>
      </field>
    </record>

    <!-- * Search view * -->
    <record id="recycling_by_campaigns_search_view" model="ir.ui.view">
      <field name="name">waste_control.recycling_by_campaigns.search</field>
      <field name="model">waste_control.recycling_by_campaigns</field>
      <field name="arch" type="xml">
        <search>
          <filter string="Activo" name="filter_active" domain="[('active', '=', True)]" />
          <filter string="Inactivo" name="filter_inactive"
            domain="[('active', '=', False)]" />

          <filter string="Año actual" name="year_current"
            domain="[('date','&gt;=', (context_today()).strftime('%Y-01-01')), ('date','&lt;=', (context_today()).strftime('%Y-12-31'))]" />

          <filter string="Año anterior" name="year_last"
            domain="[('date','&gt;=', (context_today() - relativedelta(years=1)).strftime('%Y-01-01')), ('date','&lt;=', (context_today() - relativedelta(years=1)).strftime('%Y-12-31'))]" />

          <separator />
          <filter string="Mis registros" name="filter_my_records"
            domain="[('create_uid', '=', uid)]" />

          <group expand="0" string="Agrupado por">
            <filter string="Estado" name="group_by_active"
              context="{'group_by': 'active'}" />
            <filter string="Usuario responsable" name="group_by_user"
              context="{'group_by': 'create_uid'}" />
            <filter string="Fecha de creación" name="group_by_create_date"
              context="{'group_by': 'create_date:month'}" />
          </group>
        </search>
      </field>
    </record>

    <!-- * Action * -->
    <record id="recycling_by_campaigns_act_window" model="ir.actions.act_window">
      <field name="name">Reciclaje por campañas</field>
      <field name="type">ir.actions.act_window</field>
      <field name="res_model">waste_control.recycling_by_campaigns</field>
      <field name="context">{'search_default_year_current': 1}</field>
      <field name="view_mode">tree,kanban,form,pivot</field>
      <field name="search_view_id" ref="recycling_by_campaigns_search_view" />
    </record>
  </data>
</odoo>